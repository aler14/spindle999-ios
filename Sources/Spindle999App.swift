import SwiftUI

// Entry point. Milestone 1 only needs to prove the build → sideload → run
// pipeline works on the device; real features come once that is green.
@main
struct Spindle999App: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
